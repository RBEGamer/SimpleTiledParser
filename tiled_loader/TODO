TODO



beim laden der tilesets alle weiteren tilesets erstellen in einem binary tree (als template)
zusätlich zur nomalen id auch normale ids für den text altas bauen
normale text atlase ids für das eine image bauen

beim layerdata laden passende pointer auf das tile erstellen

beim laden der animationen auch statt id auch das passende tile pointern

debug define erstellen und dann alles ausgeben